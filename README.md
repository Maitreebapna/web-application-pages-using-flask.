# web-application-pages-using-flask.
• Using a standard flask template, I have created an application that displays an index page as the
first webpage. The index page should have an HTML form with two radio buttons: one with the label
“Student ID” and the other with the label “Course ID”. It must also have an input text field to enter
the ID and a button with label “Submit”.
